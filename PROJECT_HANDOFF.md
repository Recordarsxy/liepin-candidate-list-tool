# 生活电脑实施交接说明

## 本次交接状态

工作电脑只负责冻结方案并同步文档。当前没有实现浏览器扩展、后台程序或钉钉应用，也没有把任何候选人信息、Cookie、令牌或 AppSecret 放进 GitHub。

生活电脑上的 Codex 应把以下两份文件视为唯一实施基线：

1. `docs/superpowers/specs/2026-07-31-liepin-candidate-collector-design.md`
2. `docs/superpowers/plans/2026-07-31-liepin-candidate-collector.md`

## 开始实施时的固定指令

在生活电脑克隆仓库并打开后，可以直接对 Codex 说：

> 阅读 PROJECT_HANDOFF.md、设计规格和实施计划。创建 codex/liepin-v1 分支，严格按计划逐项实施猎聘 v1；先完成钉钉权限探针和匿名页面夹具，不改变已冻结的业务规则。任何真实候选人数据、Cookie、密钥和日志都不得提交到 GitHub。

实施应使用 `subagent-driven-development` 或 `executing-plans` 工作流，并在每个里程碑运行对应测试。若实际猎聘 DOM 或钉钉权限与设计不符，应先记录证据、更新设计文档并让用户确认，不应静默改变规则。

## 已确认事实

- 工作环境是 Windows 和 Chrome，猎聘账号为普通猎头权限。
- 现场检查的猎聘列表页每页存在 30 个结构化候选人卡片，卡片位于 `table.new-resume-card`。
- 卡片中可见脱敏姓名、年龄、学历、地点、工作和教育摘要；复选框值提供稳定的 23 字符候选人标识。
- 详情页可补充性别和完整教育经历，但姓名仍是脱敏显示；测试打开详情页时没有消耗资源提示。
- Chrome 的 HKCU/HKLM 未发现企业策略键，因此暂按允许加载本地扩展处理。
- 钉钉开发者账号可创建企业内部应用，但尚未验证普通钉钉表格的具体读写权限。
- 现有钉钉候选人表的九列已经由截图确认；公司清单也在同一个现有表格体系内。

## 不得改变的产品规则

- v1 只做猎聘。脉脉以后通过单独适配器扩展；领英不复用自动采集方案。
- 用户手动搜索公司、选择关键词并翻页；工具只读取用户当前可见页面。
- 当前公司必须匹配规范公司名、已登记别名或明确分支机构。
- 只看最新一段在职经历；历史岗位命中不收录。
- 岗位排除词优先于纳入词。
- 每家公司经过人工复核后才同步；未复核数据不得进入钉钉主表。
- 不采集联系方式，不点击联系方式按钮，不解锁完整姓名。
- 不读取、导出或保存猎聘 Cookie，不调用私有接口，不拦截请求，不绕过验证码或访问限制。
- 本地运行数据不跨电脑同步，GitHub 只保存代码、匿名测试夹具和文档。
- 项目结束后删除钉钉内部应用、扩展、后台程序、密钥、本地数据库和日志。

## 首个实施检查点

开始写完整功能前，先创建一个临时钉钉企业内部应用并完成最小权限探针：

1. 确认能否读取公司清单所在工作表。
2. 确认能否向测试工作表追加一行并读取回来。
3. 确认能否创建或使用独立的“系统索引”工作表。
4. 删除测试行；把所需权限名称和申请路径写入文档。
5. 若权限不可得，直接启用 TSV 复制粘贴路径，不能让钉钉 API 阻塞 v1。

临时应用及其测试数据只能在用户明确允许的企业范围内创建；项目完成后必须删除。
