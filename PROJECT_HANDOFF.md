Exit code: 0
Wall time: 0.8 seconds
Output:
# 项目交接说明

## 当前状态

本次交接时仓库只包含设计和实施计划，尚未实现应用程序；这是历史基线，而非对后续实施的限制。请以以下两份更新后的文档作为唯一实现依据：

1. `docs/superpowers/specs/2026-07-31-liepin-candidate-collector-design.md`
2. `docs/superpowers/plans/2026-07-31-liepin-candidate-collector.md`

实施前在 Windows + Chrome 的受控环境创建并使用 `codex/liepin-v1` 分支。真实候选人信息、Cookie、密钥、日志和导出文件均不得进入 Git。

## 已冻结的产品边界

- V1 是共享候选人库，既服务完整的九列人才 List 收集，也服务岗位专属推荐。
- 平台仅限猎聘和脉脉。LinkedIn 明确排除：其官方规则禁止自动化 LinkedIn 扩展。
- 用户手动搜索、翻页和导航；工具只在用户点击后读取当前页的可见 DOM。禁止自动搜索、自动翻页、私有端点、请求拦截、Cookie 读取、验证码绕过、平台收藏、联系操作或点击联系按钮。
- 所有通过“金融渠道／机构／同业销售”宽口径人才池规则的候选人都进入详情队列。被岗位推荐的候选人优先接受深度岗位分析；其余候选人仍须完成九列补全。
- 岗位需求来自用户粘贴的聊天文本。解析结果是待用户批准的规则草案；批准后形成版本化的活动岗位规则。每次扫描都要评估全部活动岗位，并展示前 1–3 个匹配岗位。
- 所有判断仅使用 `recommend`、`reject`、`needs_review`。每项判断必须有 1–3 条支持证据或明确反证。
- 脉脉先进行用户触发的可见 DOM 解析；失败时解析用户从当前页手动复制的文本。回退模式中缺失的字段必须留空并标记，严禁猜测。

## 钉钉交付方式

主表固定九列顺序如下，且不可添加技术字段：

`目前公司～姓名／性别～年龄／目前地点～期望地点／目前岗位～硕士学校／本科学校`

V1 只把数据行以无表头 TSV 复制到剪贴板，由用户粘贴进钉钉。V1 不使用钉钉 API，不做权限探针，也不创建钉钉内部应用。

## 安全和 AI 约束

- 本地数据库保存结构化职业证据，不存完整简历页面文本。
- OpenAI 使用 Responses API，默认 `gpt-5.6-terra`、`reasoning effort: medium` 与结构化输出。发送前移除姓名、平台 ID、联系方式和 URL。
- API key 使用 Windows DPAPI。日志只能保存匿名标识、数量、状态和错误类别；不得包含候选人文本、凭证、Cookie、请求体或任何秘密。
- API 不可用时，硬规则和字段抽取继续；需要复杂语义判断的岗位结论必须变为 `needs_review`。

## 首次实施的验收重点

先完成核心数据/规则、猎聘采集与两条工作流，再实现脉脉。最终在真实页面上、由用户手动触发，验证：两平台的可见 DOM 采集、脉脉文本回退、全部活动岗位匹配、推荐优先深度分析、其余候选人的九列补全、无表头 TSV 复制、隐私脱敏与失败降级。详细步骤、测试和提交节奏在实施计划中。
