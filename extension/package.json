{
  "name": "candidate-store-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest --environment jsdom",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "npm run typecheck && node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^26.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.3.6",
    "vitest": "^3.2.0"
  },
  "vitest": {
    "environment": "jsdom"
  }
}
